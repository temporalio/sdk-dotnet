namespace Temporalio.Common
{
    /// <summary>
    /// Constants needed by the Temporal SDK.
    /// </summary>
    internal static class Constants
    {
        /// <summary>
        /// Prefix for reserved handler and definition names.
        /// </summary>
        internal const string ReservedNamePrefix = "__temporal";
    }
}
